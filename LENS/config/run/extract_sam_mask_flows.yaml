flows:
-   object_type: flow
    name: Refine_segment_by_sam3
    unit: frame
    processes:
    -   object_type: segment
        text: Top-down view, solid plastic body only, ignore holes, ignore shadow,
            ignore conveyor background.
        conf: 0.0
        min_area: 200
        model:
            type: sam3
            bpe_path: ''
            checkpoint: ''
            device: cuda
        outputs:
            object: {}          # box 하나당 best mask → 객체의 mask(rle). 라벨맵 저장 안 함
