"""gui/meta_page/analysis — 형상 적합성 **검증** 창 (비모달).

`core.analysis` 를 부르고 그 산출물을 보이며, 결과를 보고 **class 를 옮기는** 것까지가 이 창이다 —
**계산은 여기서 안 한다**. 창을 좌우로 가른 축이 그대로 파일이 갈린 축이다:

===========  ============================================================
파일         무엇
===========  ============================================================
`_dialog`    두 쪽의 배선 — 설정을 core 인자로 넘기고 워커로 돌린다
`_panel`     좌: 무엇을 분석할지 정하고 국면을 시킨다 (값·신호만)
`_view`      우: 산출물을 보인다 (type 구성 · 근접 · 형상)
===========  ============================================================
"""

from ._dialog import Analysis_dialog

__all__ = ["Analysis_dialog"]
