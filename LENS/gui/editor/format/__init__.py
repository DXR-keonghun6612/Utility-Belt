"""편집기가 다루는 **좌표 데이터의 표현들** — 도구보다 먼저 있고, 도구는 이것을 만들고 고칠 뿐이다.

한 format 은 "무엇을 담는가"(꼭짓점 목록 · 두 코너)와 그 위의 순수 연산(히트 판정 · 정규화 · 변환)만
안다. **모르는 것이 이 계층의 정의다** — cv2 도 Qt 도 `Target` 도 편집기도 모른다. 그래서 같은 상자를
2d 라벨링과 3d 뷰포트가 함께 쓰고, 같은 폴리곤을 *저장하는 쪽*과 *픽셀로 찍는 쪽*이 함께 쓴다.

| format | 정준형 | 저장 표현과의 경계 |
|---|---|---|
| [`bbox`](bbox.py) | ``(2, D)`` — ``[lo, hi]`` | `from_flat`/`to_flat` (COCO ``[x0,y0,x1,y1]``) |
| [`polygon`](polygon.py) | ``(N, D)`` — 꼭짓점 | `from_flat`/`to_flat` (COCO ``segmentation``) |

**규칙 셋** (새 format 을 더할 때도 같다):

1. **numpy 로 차원을 안 박는다.** x·y 를 이름으로 쓰는 순간 3d 에서 못 쓴다 — `D` 는 배열이 든다.
2. **평탄 좌표열은 경계에서만.** 저장 표현(COCO 평탄 리스트)을 안으로 들이면 "길이가 4"가 곳곳에 박힌다.
3. **자기 활용법을 모른다.** 폴리곤이 자기를 래스터화할 줄 알면 그 순간 올가미 편이 되어, 저장되는
   쪽과 찍히는 쪽 양쪽에 같이 설 수 없다 — 래스터화는 [`image/_region.py`](../image/_region.py) 가 든다.

라스터 전용인 **region**(찍을 픽셀 집합)이 여기 없는 것이 규칙 3 의 결과다. 그것만은 저장되지 않고
(찍힌 *결과*가 데이터다) cv2 에 묶여 있어 `image/` 에 산다.
"""
