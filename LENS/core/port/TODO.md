# TODO — port

완료 이력은 git, 최종 설계는 [`README.md`](README.md). 상위 계획은 [`../TODO.md`](../TODO.md) "★ core 4분할".

**이 계층의 계약** — 데이터 **하나**의 실체화(읽기/쓰기)와 외부 세계 발견. `schema` 만 안다.
**`store` 를 모르고, `store` 만이 부른다.** ([`../test_layering.py`](../test_layering.py) 가 강제)

---

## ✅ 결론 난 논의 — `pkgutil` eager 자동등록 → **더 이상 문제 아님** (2026-07-12)

> README 승격 대기 (문서 패스).

전 핸들러를 import 시점에 통째로 올리는 게 문제였던 **유일한 이유는 cv2-free 를 무력화한다는 것**이었다.
`Data_Ref` 가 [`../schema.py`](../schema.py) 로 빠진 지금 **port 는 떳떳하게 무거워도 된다** — 원래
디스크·포맷 계층이다. 순수해야 하는 건 `schema` 한 모듈이지 이 계층이 아니었다.

(원칙: `claude-knowledge` 계층① "순수성은 모듈에 걸고 계층에 걸지 않는다".)

**단, 자동등록 순회에 딸린 제약이 하나 생겼다** — 등록 대상이 아닌 모듈(`scan.py`)이 이 패키지에 섞여
있어도 무해하지만(데코레이터가 없으면 아무 일도 안 일어난다), 그런 모듈은 **port 내부를 import 하면
안 된다**. 순회가 도는 중이라 순환이 난다. `scan.py` 가 pathlib 만 아는 이유가 이것이다.

---

## ❓ 논의 대상

- [ ] **`docs` 핸들러의 자리** — id_map 같은 중첩 구조(yaml/json)를 담는데, 이건 payload 라기보다
      **구조 사이드카에 가깝다**(`Structure` 와 역할이 겹친다). 둘의 경계를 정한다.
