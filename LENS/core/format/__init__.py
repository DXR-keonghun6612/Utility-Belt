"""format — **데이터 구조와 그 연산.** 자기가 무슨 뜻인지도, 어디 사는지도 모른다.

축이 둘이라는 건 [`../port`](../port) 가 이미 정했지만(``format = (domain, format)``), 축의 **뜻**이
섞여 있었다 — 도메인이 **대표 포맷 하나**(정준형)를 정해 모든 포맷을 그리로 뭉갰고, 그래서 폴리곤을
읽으면 배열이 나왔다. 구조가 도메인에 먹힌 것이다. 이 계층이 그 구조를 되찾아 온다.

| 층 | 아는 것 | 모르는 것 |
|---|---|---|
| `format/` (여기) | 구조와 그 연산 — 채우기·외곽선·정규화 | 그게 무슨 뜻인지 · 어디 사는지 |
| [`../codec`](../codec) | 그 구조의 입출력 | 바이트가 무슨 뜻인지 |
| [`../port`](../port) (domain) | **무엇으로 읽나** — 채울까 그을까 | (둘 다 안다 — 유일하게) |

*codec 은 바이트가 무슨 뜻인지 모르고, format 은 그게 어디 사는지 모르고, domain 만 둘 다 안다.*

## 왜 연산이 여기 있고 codec 에 없나

`polygon` 구조는 **자기가 면인지 선인지 모른다.** 채우면 mask 고 그으면 polyline 이다 — 그 선택은
도메인의 말이지 구조의 말도, I/O 의 말도 아니다. 그래서 여기는 `Fill` 을 *제공만* 하고 **고르지 않는다**.
한때 이게 `codec/polygon.py::Decode_polygon` 이었는데, 이름의 "Decode"(= 정준형으로 푼다)가 곧 "폴리곤은
언제나 픽셀이다"라는 결정이었다 — I/O 모듈이 의미를 정하고 있었던 것.

## 규칙

1. **`Data_Ref` 도 I/O 도 모른다.** numpy·cv2 는 쓴다 — 순수해야 하는 건 [`../schema.py`](../schema.py)
   한 모듈이지 이 계층이 아니다.
2. **저장 표현(평탄 좌표열)은 경계에서만.** COCO 의 ``[x0,y0,x1,y1]`` 을 안으로 들이면 "길이가 4"가
   곳곳에 박힌다.
3. **뜻을 고르지 않는다.** 변환을 *제공*할 뿐, 어느 것을 쓸지는 도메인이 정한다.
"""
